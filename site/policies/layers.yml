# Clean, inward-pointing dependency rule across layers.
order: [domain, application, interface, infra]

packages:
  kgfoundry_common: domain
  kg_builder: application
  orchestration: application
  search_api: interface
  embeddings_dense: infra
  embeddings_sparse: infra
  ontology: infra

rules:
  allow_same_layer: true
  allow_outward: false

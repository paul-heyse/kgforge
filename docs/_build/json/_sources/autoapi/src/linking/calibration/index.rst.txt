src.linking.calibration
=======================

.. py:module:: src.linking.calibration

.. autoapi-nested-parse::

   Module for linking.calibration.

   NavMap:
   - NavMap: Structure describing a module navmap.
   - isotonic_calibrate: Calibrate score/label pairs using an isotonic regression….





Module Contents
---------------

.. py:function:: isotonic_calibrate(pairs)

   Calibrate score/label pairs using an isotonic regression placeholder.

   :param pairs: Sequence of ``(score, label)`` pairs where score is in ``[0, 1]`` and
                 label is either ``0`` or ``1``.
   :type pairs: list[tuple[float, int]]

   :returns: Calibration artefact describing the fitted model (placeholder).
   :rtype: dict[str, object]



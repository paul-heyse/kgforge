# Modules

Module summaries are generated automatically during the MkDocs build via the
``mkdocs-gen-files`` plugin. Run ``mkdocs build`` using this suite to populate
the index and per-module pages.

